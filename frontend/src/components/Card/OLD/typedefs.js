/**
 * @typedef CardProps
 * @prop {number} [width = 200]
 * @prop {number} [height = 300]
 * @prop {number} [ratio = 3 / 2]
 * @prop {string} title
 * @prop {string} [subtitle]
 * @prop {string} img_url
 * @prop {string} [bg_url]
 * @prop {CardEventOptions} [eventOptions]
 */

/**
 * @typedef CardWrapperProps
 * @prop {number} cardWidth
 * @prop {number} cardHeight
 * @prop {CardEventOptions} eventOptions
 * @prop {React.JSX.Element[]} children
 */

/**
 * @typedef CardContentPros
 * @prop {string} title
 * @prop {string} [subtitle]
 * @prop {string} img_url
 * @prop {string} [bg_url]
 */

export {};
